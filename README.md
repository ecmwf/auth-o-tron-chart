This repository holds the Helm chart for deploying [Auth-o-tron](https://github.com/ecmwf/auth-o-tron).

You have the option of deploying a complementary MongoDb instance (off by default) or providing a link to an existing one.

You need to provide the imageCredentials parameters as in [values.yaml] to deploy your built image of auth-o-tron.