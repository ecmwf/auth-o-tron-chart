apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-o-tron-chart.fullname" . }}
  labels:
    {{- include "auth-o-tron-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auth-o-tron-chart.selectorLabels" . | nindent 4 }}
