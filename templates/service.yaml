apiVersion: v1
kind: Service
metadata:
  name: auth-o-tron-svc
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: {{ include "fullname" . }}